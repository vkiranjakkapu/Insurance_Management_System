server:
  port: 8888
spring:
  application:
    name: IMS-CONFIGSERVER
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:${user.dir}/../repo